class InventoryRequest {
  final String name;
  final String address;  // change category and quantity to address only

  InventoryRequest({
    required this.name,
    required this.address,
  });
}
